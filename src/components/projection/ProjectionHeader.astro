<div class="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between">
  <div>
    <p class="text-label-caps font-label-caps text-primary">
      Planning workspace
    </p>
    <h1 class="mt-1 text-headline-lg font-bold text-on-surface">
      Compound Interest Projection
    </h1>
    <p class="mt-1 max-w-2xl text-body-md text-on-surface-variant">
      Model how your starting capital and monthly contributions can grow over
      time.
    </p>
  </div>
  <div class="flex flex-wrap gap-3">
    <span
      id="save-status"
      class="self-center text-xs font-semibold text-on-surface-variant"
      aria-live="polite"></span>
    <button
      id="save-projection"
      class="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2 text-label-caps font-label-caps text-on-primary hover:opacity-90"
    >
      <span class="material-symbols-outlined text-base">save</span>
      Save projection
    </button>
  </div>
</div>
