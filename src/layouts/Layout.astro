---
import '@fontsource/inter/400.css';
import '@fontsource/inter/500.css';
import '@fontsource/inter/600.css';
import '@fontsource/inter/700.css';
import '@fontsource-variable/jetbrains-mono';
import '../styles/global.css';
import { IconGrid, IconCreditCard, IconClock, IconTrendingUp } from '../components/icons';

interface Props {
  title: string;
  activePage?: string;
}

const { title, activePage = 'dashboard' } = Astro.props;

const navItems = [
  { href: '/', label: 'Dashboard', icon: IconGrid, page: 'dashboard' },
  { href: '/accounts', label: 'Accounts', icon: IconCreditCard, page: 'accounts' },
  { href: '/history', label: 'History', icon: IconClock, page: 'history' },
  { href: '/wealth', label: 'Wealth Management', icon: IconTrendingUp, page: 'wealth' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Financial Records Manager - Personal Finance Management" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Financial Records Manager</title>
  </head>
  <body class="min-h-screen bg-surface">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-64 bg-white border-r border-outline-variant flex flex-col">
        <!-- Logo -->
        <div class="p-6 border-b border-outline-variant">
          <h1 class="text-headline-md font-bold text-primary">Financial Records</h1>
          <p class="text-body-md text-on-surface-variant mt-1">Manager</p>
        </div>
        
        <!-- Navigation -->
        <nav class="flex-1 p-4 space-y-1">
          {navItems.map((item) => (
            <a
              href={item.href}
              class:list={[
                'flex items-center px-4 py-3 rounded-lg text-sm font-medium transition-colors',
                activePage === item.page
                  ? 'bg-primary text-on-primary'
                  : 'text-on-surface-variant hover:bg-surface-container-low hover:text-on-surface',
              ]}
            >
              <item.icon class="w-5 h-5 mr-3" />
              {item.label}
            </a>
          ))}
        </nav>
        
        <!-- User section -->
        <div class="p-4 border-t border-outline-variant">
          <div class="flex items-center">
            <div class="w-8 h-8 bg-primary rounded-full flex items-center justify-center">
              <span class="text-on-primary text-sm font-medium">JD</span>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-on-surface">John Doe</p>
              <p class="text-xs text-on-surface-variant">Personal Account</p>
            </div>
          </div>
        </div>
      </aside>
      
      <!-- Main content -->
      <main class="flex-1 overflow-auto">
        <div class="max-w-container mx-auto p-gutter">
          <slot />
        </div>
      </main>
    </div>
  </body>
</html>
